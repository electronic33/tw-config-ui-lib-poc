import React from 'react';
import { Button } from '../src';

export default {
  title: 'Button',
  component: Button,
};

export const Default = (): React.ReactNode => <Button />;
export const Default2 = (): React.ReactNode => <Button />;
